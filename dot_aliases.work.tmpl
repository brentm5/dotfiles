{{- if .work_environment }}
# Work-specific aliases
# This file only exists on work machines

# Example work aliases:
# alias deploy='kubectl apply -f'
# alias vpn-connect='sudo openconnect --protocol=gp vpn.company.com'
# alias work-logs='tail -f /var/log/work-app.log'
# alias work-ssh='ssh user@work-server.com'

# Add your work aliases here

{{- end }}